Place your app icons here before running `npm run build`:

- icon.png  (512x512, Linux)
- icon.ico  (Windows)
- icon.icns (macOS)

electron-builder reads these paths directly (see the "build" section of package.json).
